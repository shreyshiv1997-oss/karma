# KARMA native — no reflection-based libraries in use (org.json + HttpURLConnection only),
# so the default rules are sufficient. Keep the switch if minify is ever enabled.
-keepattributes *Annotation*
