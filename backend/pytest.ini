[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    timeout: a per-test wall-clock budget, enforced only when pytest-timeout is installed (see tests/test_alembic.py)
